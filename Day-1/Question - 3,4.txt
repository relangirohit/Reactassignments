3. Keys in React are kind of string attributes used when creating lists of elements in react. And are used to
   identify which items in the list are changed,updated or deleted.

   let resultsArray = usersArray.map((item, index) => {
        const arr = [];
	for (let i = 0; i < item.rating; i++) {
    		arr.push(
			<>⭐</>
    		);
	}

4. <img src="./images/ro.jpg" alt="logo" />